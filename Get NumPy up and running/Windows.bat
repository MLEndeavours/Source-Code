:: The native way
pip install numpy

:: If above fails, download get-pip.py
:: from https://bootstrap.pypa.io/get-pip.py
:: Now, change to the the directory where you just downloaded it and do this-
python get-pip.py

:: Using Anaconda. Search for "Anaconda Prompt" and
:: run it. Then type in-
conda update numpy
